To run this project
clone or download it

Run newtribes.py


Dependencies:
neo4j
pandas
json
codecs

A database should be created and running in neo4j.
If the database has different url or username or password than the url in the script, update the url,
username and the password in the script accordingly
